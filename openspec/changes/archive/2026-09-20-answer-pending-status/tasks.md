## 1. Модель экрана

- [x] 1.1 Добавить в `src/chgk_agent/ui/view.py` текст статуса «Думаю...» и построение модели экрана для состояния выполнения; проверить тестом, что модель не пуста и отличима от ответа «Не знаю»
- [x] 1.2 Убедиться, что текст статуса не совпадает с `UNKNOWN_ANSWER` и не попадает в проверку `is_unknown`; проверить тестом на построение модели из ответа API

## 2. Страница

- [x] 2.1 Показывать статус «Думаю...» перед обращением к поиску и заменять его ответом после получения; проверить тестом порядок «статус → ответ» с клиентом-заглушкой, который фиксирует состояние на момент вызова
- [x] 2.2 Убедиться, что ответ на предыдущий вопрос исчезает до появления нового; проверить тестом с двумя последовательными вызовами обработчика
- [x] 2.3 Блокировать кнопку и поле ввода на время запроса и разблокировать их в `finally`; проверить тестом, что неожиданная ошибка клиента не оставляет форму заблокированной, а экран показывает «Не знаю»
- [x] 2.4 Заменить `bind_enabled_from` на явное условие «описание достаточно длинное и запрос не выполняется» и применять его к кнопке и полю; проверить тестом, что изменение значения поля во время запроса не разблокирует форму
- [x] 2.5 Проверить, что при пустом или слишком коротком описании статус не показывается и сразу выводится «Не знаю»; проверить тестом с клиентом, который фиксирует отсутствие вызовов

## 3. Проверка

- [x] 3.1 Обновить тесты UI под новое поведение и убедиться, что `uv run pytest -q` проходит
- [x] 3.2 Прогнать `uv run ruff check src tests` без замечаний
- [x] 3.3 Проверить вручную на живом сервере: нажать «Ответить» и убедиться, что прежний ответ исчезает, появляется «Думаю...», кнопка и поле ввода заблокированы, затем статус заменяется ответом и форма разблокируется
