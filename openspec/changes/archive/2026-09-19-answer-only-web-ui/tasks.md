## 1. Генерация ответа

- [x] 1.1 Заменить `GENERATION_SYSTEM_PROMPT` на ролевой промпт игрока ЧГК и переписать `_generation_prompt` так, чтобы список похожих вопросов был необязательным; проверить `tests/test_search_graph.py::test_generate_uses_matches_and_reports_sources` и добавить тест на пустой список вопросов
- [x] 1.2 Разрешить проход к генерации без совпадений: убрать в `needs_generation` и `format_response` условие «нет подтверждающих совпадений»; проверить тестами, что граф вызывает генерацию при пустой выдаче и больше не возвращает `error="нет подтверждающих совпадений"`
- [x] 1.3 Поднять `SearchSettings.min_score` по умолчанию до `0.85` и обновить `CHGK_SEARCH__MIN_SCORE` в `.env.example`; проверить `tests/test_config.py` и тест значения по умолчанию

## 2. Веб-интерфейс

- [x] 2.1 Переписать страницу `/ui`: оставить поле описания, кнопку «Ответить» и область ответа, убрать панель параметров, статусы, карточки совпадений и блок «опирается на найденные вопросы»; проверить `test_ui_page_is_mounted` и добавить тест состава страницы
- [x] 2.2 Заменить `TITLE` на «ЧГК знаток» и убедиться, что он попадает и в `<title>`, и в заголовок страницы, и в `ui.run_with`; проверить тестом значение `TITLE`
- [x] 2.3 Заблокировать кнопку, пока описание короче трёх символов, и добавить повторную проверку в обработчике, возвращающую «Не знаю» без обращения к API; проверить тестами границы длины и обход блокировки
- [x] 2.4 Отвечать «Не знаю» при ошибке поиска и при недоступной генерации, не выводя `request_id` и текст ошибки на страницу; проверить тестами на `SearchApiError` и `answer.available = false`
- [x] 2.5 Удалить из `src/chgk_agent/ui/view.py` и `src/chgk_agent/ui/app.py` неиспользуемые отображение совпадений и источников вместе с их тестами; проверить `ruff check src tests` и отсутствие мёртвых ссылок в `__all__`

## 3. Проверка

- [x] 3.1 Обновить тесты UI и графа под новое поведение и убедиться, что `uv run pytest -q` проходит без пропущенных новых кейсов
- [x] 3.2 Проверить сквозной сценарий из `tests/test_end_to_end.py`: запрос с совпадениями и запрос без совпадений оба дают непустой ответ; запустить `-m integration`
- [x] 3.3 Обновить `README.md`: описание интерфейса «вопрос — ответ», новый заголовок, отсутствие показа похожих вопросов и значение порога по умолчанию
- [x] 3.4 Проверить страницу `/ui` вручную после перезапуска `chgk-serve`: поле, кнопка «Ответить», ответ на вопрос и «Не знаю» при недоступной генерации

## 4. Известная проблема (вне рамок изменения)

- [x] 4.1 Зафиксировать в issue или README найденный сбой парсера внешнего источника `invalid literal for int() with base 10: ''`; отдельного исправления в этом изменении не требуется
