## 1. Единый критерий устаревания

- [x] 1.1 Дополнить выборку устаревших векторов проверкой размерности: считать вопрос подлежащим переиндексации, если `embedding` пуст, `embedding_model` не совпадает с текущей моделью или `embedding_dim` не совпадает с текущей размерностью; проверка — тест подтверждает, что вопрос с верной моделью и неверной размерностью попадает в выборку и в счётчик
- [x] 1.2 Убедиться, что критерий совпадает с условиями семантической ветки поиска (`embedding IS NOT NULL`, совпадение модели и размерности); проверка — тест находит вопрос после переиндексации и не находит до неё

## 2. Обычный запуск и принудительный полный пересчёт

- [x] 2.1 Сделать обычный запуск `chgk-reindex` обработкой отсутствующих и устаревших векторов; проверка — после смены модели команда без флага пересчитывает устаревшие векторы и не трогает актуальные
- [x] 2.2 Реализовать `--force` как разовый полный пересчёт всех вопросов за один проход без цикла до исчерпания очереди; проверка — тест подтверждает ровно один вызов выборки по всем вопросам и отсутствие повторного прохода
- [x] 2.3 Сделать ветку выборки без модели рабочей и использовать её в принудительном режиме; проверка — тест подтверждает, что принудительный режим пересчитывает и вопросы с актуальной моделью и размерностью
- [x] 2.4 Привести отчёт в порядок: `remaining` в обычном режиме — размер очереди, в принудительном — число неуспешных за проход; проверка — тест проверяет оба режима, включая `--json`

## 3. Удаление выбора источника

- [x] 3.1 Убрать флаг `--location` из парсера `chgk-reindex`; проверка — тест подтверждает, что неизвестный флаг отклоняется, а запуск без флага работает
- [x] 3.2 Убрать параметр `location` из `embed_pending`, `reindex_once`, `drain_queue` и функций репозитория очереди и переиндексации; проверка — `uv run ruff check src` не находит неиспользуемых параметров, а полный прогон тестов проходит
- [x] 3.3 Обновить тесты, использовавшие `location` для ограничения своих данных: сохранить очистку закоммиченных строк и убрать зависимость точных счётчиков от порядка запуска; проверка — тесты проходят при запуске как по отдельности, так и в полном наборе

## 4. Документация

- [x] 4.1 Переписать в README раздел о смене модели эмбеддингов: обычный запуск сам обрабатывает устаревшие векторы, `--force` — полный пересчёт, `--location` удалён; проверка — в README нет упоминаний `--location`, а описание соответствует поведению команды
- [x] 4.2 Отметить в README, что рецепт смены размерности со сбросом схемы через `alembic downgrade` удаляет импортированные вопросы; проверка — предупреждение присутствует рядом с командой

## 5. Проверка

- [x] 5.1 Прогнать `uv run ruff check src tests` и `uv run pytest -q`; проверка — оба прохода без ошибок
- [x] 5.2 Проверить поведение команд на отдельной базе, не трогая рабочую: обычный запуск `chgk-reindex` не пересчитывает ничего на согласованной базе, `--force` пересчитывает все вопросы за один проход; проверка — отчёты обеих команд соответствуют ожиданию, а рабочая база побайтово не изменилась
