## 1. Общий текст и эмбеддинг описания

- [x] 1.1 Ввести одну функцию построения текста для эмбеддинга из вопроса и ответа и применить её в импорте, переиндексации и поиске; проверка — юнит-тест показывает, что функция даёт одинаковый результат на паре «вопрос, ответ» независимо от вызывающей стороны
- [x] 1.2 Нормализовать описание перед вычислением эмбеддинга тем же правилом, что и документы, и считать эмбеддинг описания один раз за поиск; проверка — тест на поиске подтверждает один вызов провайдера эмбеддингов на запрос и совпадение нормализованного текста с ожидаемым
- [x] 1.3 Перевести векторизацию при импорте и в переиндексации на текст «вопрос + ответ»; проверка — интеграционный тест подтверждает, что сохранённый эмбеддинг посчитан от склеенного текста, а не только от вопроса

## 2. Единая оценка и симметричный лексический сигнал

- [x] 2.1 Добавить зависимость `snowballstemmer` и общий модуль токенизации со стеммингом русских слов; проверка — юнит-тест показывает, что «галстуки», «галстуков» и «галстук» дают одну основу, а «учёного» и «ученики» не сходятся в одну основу
- [x] 2.2 Реализовать BM25 по тексту «вопрос + ответ» с нормировкой насыщением и коллекцией из локального корпуса плюс карточки внешнего источника; проверка — тест подтверждает, что редкий термин даёт больший вклад, чем частый, а оценка лежит в допустимом диапазоне
- [x] 2.3 Построить локальный BM25-индекс по всему корпусу и кэшировать его с инвалидацией при изменении корпуса; проверка — тест подтверждает, что индекс строится один раз на несколько запросов, а после импорта или переиндексации перестраивается
- [x] 2.4 Считать лексический сигнал и итоговую оценку в одном общем узле после объединения веток, чтобы обе стороны видели одну коллекцию; проверка — тест сравнивает сигнал для одного текста, поданного как локальная запись и как внешняя карточка, и значения совпадают
- [x] 2.5 Взять лексических кандидатов из полного индекса независимо от семантической ветки, чтобы лексика добавляла recall; проверка — тест находит вопрос, которого нет в семантической выдаче, но который содержит редкий термин описания
- [x] 2.6 Собрать итоговую оценку как `cos + вес * BM25` с весом и параметром насыщения из настроек; проверка — тест подтверждает порядок выдачи и то, что оценка лежит в допустимом диапазоне
- [x] 2.7 Применять пользовательский порог к единой величине и убрать выбор порога по типу оценки; проверка — тест подтверждает, что порог отсекает совпадения обоих источников по одной и той же величине, а понижение порога не меняет порядок остальных
- [x] 2.8 Сохранить деградацию, когда индекс недоступен: поиск работает по семантической близости и помечает ответ частичным; проверка — тест подтверждает частичный ответ и отсутствие ошибки при недоступном или непрогретом индексе
- [x] 2.9 Удалить лексическую ветку на `websearch_to_tsquery` и `ts_rank_cd` вместе с `_normalize_rank` и `_as_ranked_items`; проверка — `rg` не находит этих вызовов в `src`, полный прогон тестов проходит

## 3. Снятие RRF и позиционного ранжирования

- [x] 3.1 Удалить reciprocal rank fusion: модуль, его тесты, экспорт и гибридную логику локального поиска; проверка — `rg` не находит упоминаний RRF в `src` и тестах, полный прогон тестов проходит
- [x] 3.2 Исключить позиционное затухание внешней выдачи из оценки, сохранив позицию только как диагностику в отчёте об источнике; проверка — тест подтверждает, что оценка внешнего совпадения не зависит от позиции, а позиция по-прежнему доступна в отчёте
- [x] 3.3 Переосмыслить `score_kind` как признак происхождения совпадения и вклада лексического сигнала и обновить схему ответа API; проверка — тест API подтверждает новое значение `score_kind` для совпадений обоих источников

## 4. Деградация и наблюдаемость

- [x] 4.1 При сбое эмбеддинга карточек внешнего источника исключать внешние совпадения, помечать источник недоступным с отдельной причиной и возвращать частичный ответ с локальными совпадениями; проверка — тест с падающим провайдером эмбеддингов подтверждает статус источника, отсутствие внешних совпадений и сохранение локальных
- [x] 4.2 Добавить метрику недоступности эмбеддинга внешних карточек и метрику деградации лексической ветки; проверка — тест метрик подтверждает инкремент обоих счётчиков в соответствующих сценариях
- [x] 4.3 Убедиться, что исход не выдаётся за пустую выдачу; проверка — тест подтверждает статус недоступности с причиной, а не статус `empty`

## 5. Интерфейс

- [x] 5.1 Привести подпись оценки в веб-интерфейсе в соответствие с единой величиной близости и убрать выдачу ранговой оценки за семантическую близость; проверка — тест представления подтверждает подпись и процент для совпадений обоих источников
- [x] 5.2 Показать одно совпадение с указанием обоих источников при дедупликации; проверка — тест подтверждает объединение источников и единую оценку

## 6. Выкат

- [x] 6.1 Обновить README: новый текст эмбеддинга, обязательный принудительный прогон переиндексации и порядок выката; проверка — инструкция содержит шаг `chgk-reindex --force` до включения новой выдачи
- [x] 6.2 Выполнить `chgk-reindex --force` и убедиться, что неуспешных нет; проверка — отчёт CLI показывает ноль неуспешных и пустую очередь; при ненулевом значении прогон повторяется
- [x] 6.3 Проверить сквозной сценарий на запросе «галстук»: внешние совпадения со второй позиции и далее присутствуют в выдаче и ранжируются по единой оценке; проверка — сквозной тест или ручной прогон поиска с наблюдаемой выдачей
