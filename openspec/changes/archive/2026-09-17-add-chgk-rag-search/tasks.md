## 1. Фундамент проекта

- [x] 1.1 Описать структуру пакета `src/chgk_agent/` (`models`, `db`, `ingestion`, `embeddings`, `external`, `search`, `api`, `ui`) и добавить `__init__.py`; проверить, что `python -c "import chgk_agent"` выполняется без ошибок
- [x] 1.2 Добавить зависимости в `pyproject.toml` (fastapi, uvicorn, nicegui, langgraph, sqlalchemy[asyncio], asyncpg, pgvector, alembic, httpx, beautifulsoup4, lxml, pydantic-settings, prometheus-client, structlog, langchain-gigachat, tenacity, pytest, pytest-asyncio, respx) и выполнить `uv sync`; проверка — `uv run python -c "import fastapi, nicegui, langgraph, pgvector"` без ошибок
- [x] 1.3 Настроить `pydantic-settings` конфигурацию (БД, GigaChat credentials/scope/model/embedding dim, параметры внешнего источника, лимиты и таймауты) с валидацией; проверить, что внешний источник включён по умолчанию (`enabled=true`, `base_url=https://gotquestions.online`), `user_agent` обязателен и непуст, а лимит поискового запроса равен `50`; проверка — unit-тест на разбор настроек из переменных окружения, на значение `enabled=true` по умолчанию и на отказ при пустом `user_agent`
- [x] 1.4 Настроить `structlog` с прокидыванием `request_id` через `contextvars` и маскированием секретов; проверка — unit-тест на наличие `request_id` в записи и на отсутствие токена в выводе
- [x] 1.5 Настроить `pytest`/`pytest-asyncio` с `asyncio_mode=auto`, маркерами `integration` и общими фикстурами; проверка — `uv run pytest -q` проходит на пустом наборе

## 2. Схема БД и миграции

- [x] 2.1 Добавить Alembic и первую миграцию: `CREATE EXTENSION IF NOT EXISTS vector`, таблицы `sources`, `questions`, `question_occurrences` и уникальное ограничение `(source_id, source_key)`; проверка — миграция применяется на чистой базе и откатывается без ошибок
- [x] 2.2 Добавить колонку `questions.embedding vector(<embedding_dim>)`, поля `embedding_model`/`embedding_dim` и индекс HNSW; проверка — миграция применяется при настройке размерности из конфигурации, а при недостижимой размерности выдаётся понятная ошибка
- [x] 2.3 Добавить GIN-индекс полнотекстового поиска по нормализованному тексту вопроса; проверка — интеграционный тест подтверждает, что запрос полнотекстового поиска использует индекс
- [x] 2.4 Реализовать async-сессию SQLAlchemy и фабрику движка; проверка — интеграционный тест открывает сессию, выполняет запрос и корректно её закрывает
- [x] 2.5 Проверить на старте приложения совпадение размерности вектора от провайдера эмбеддингов с `vector(<embedding_dim>)`; проверка — unit-тест на несовпадение размерности даёт ошибку старта с понятным сообщением
- [x] 2.6 Подключить локально запущенный PostgreSQL с pgvector для разработки и интеграционных тестов; проверка — `pg_isready`, успешное применение миграций и прохождение интеграционных тестов, а без доступной базы они корректно пропускаются

## 3. Доменные модели и парсинг HTML

- [x] 3.1 Описать доменные модели вопроса, ответа, вхождения, источника и отчёта импорта; проверка — unit-тесты на создание моделей и на нормализацию текста
- [x] 3.2 Реализовать нормализацию текста вопроса (регистр, пробелы, типографика) и хеш нормализованного текста; проверка — unit-тесты подтверждают идемпотентность нормализации и совпадение хеша для записей, различающихся только регистром и пробелами
- [x] 3.3 Реализовать интерфейс `HtmlQuestionParser` и первый адаптер разбора HTML-файла с вопросами и ответами; проверка — unit-тесты на фикстуре реального файла извлекают все пары «вопрос — ответ»
- [x] 3.4 Обработать краевые случаи разбора: файл без распознанных пар, блок с вопросом без ответа, неполные и битые записи; проверка — unit-тесты на каждую ситуацию дают соответствующие счётчики и ошибки без частичной записи в базу
- [x] 3.5 Добавить CLI-команду разбора и импорта HTML-файлов и каталогов; проверка — запуск команды на тестовой фикстуре формирует отчёт импорта с корректными счётчиками

## 4. Клиент GigaChat на langchain_gigachat: эмбеддинги и генерация

- [x] 4.1 Добавить зависимость `langchain-gigachat` и реализовать `EmbeddingProvider` как адаптер над `GigaChatEmbeddings`: батчинг, ограничение параллелизма, таймауты и ретраи; проверка — unit-тесты с подменённой моделью подтверждают батчинг, retry и корректную обработку ошибок
- [x] 4.2 Реализовать `ChatProvider` как адаптер над chat-моделью `GigaChat` из `langchain_gigachat` с системным промптом, опирающимся только на найденные совпадения; проверка — unit-тест подтверждает, что промпт содержит переданные совпадения и не содержит посторонних данных
- [x] 4.3 Детализировать обработку ошибок и частичных сбоев: невекторизованные вопросы помечаются и не сохраняют частичный эмбеддинг, ошибка генерации не ломает выдачу совпадений; проверка — unit-тесты на ошибку по части батча и на недоступность сервиса
- [x] 4.4 Учесть использование GigaChat через единый слой `langchain_gigachat`: число вызовов, ошибок и токенов по типу операции; проверка — unit-тест подтверждает изменение счётчиков после успешного и неуспешного вызова
- [x] 4.5 Реализовать фоновый доиндексатор очереди невекторизованных вопросов; проверка — интеграционный тест: после векторизации ранее невекторизованный вопрос становится доступен семантическому поиску

## 5. Импорт и дедупликация в базу

- [x] 5.1 Реализовать идемпотентную запись вопросов и вхождений через `ON CONFLICT (source_id, source_key) DO UPDATE`; проверка — интеграционный тест: повторный импорт того же файла не увеличивает число вопросов
- [x] 5.2 Реализовать обновление версии источника при изменении текста вопроса или ответа без появления дубликатов; проверка — интеграционный тест сравнивает содержимое до и после повторного импорта изменённого файла
- [x] 5.3 Реализовать сценарий одинакового вопроса в разных источниках: отдельные вхождения с корректными ссылками на файлы; проверка — интеграционный тест подтверждает два вхождения одного канонического вопроса
- [x] 5.4 Реализовать ведение отчёта импорта (обработано, добавлено, обновлено, пропущено, невекторизовано, ошибки по файлам) и его сохранение по идентификатору операции; проверка — интеграционный тест на частично неуспешный импорт возвращает полный отчёт
- [x] 5.5 Реализовать отслеживание состояния длительной операции импорта до её завершения; проверка — интеграционный тест получает промежуточные счётчики во время выполнения

## 6. Локальный гибридный поиск

- [x] 6.1 Реализовать kNN-поиск по `embedding` с порогом и ограничением числа результатов; проверка — интеграционный тест на предзаполненной базе возвращает ожидаемый вопрос первым
- [x] 6.2 Реализовать полнотекстовый поиск по нормализованному тексту; проверка — интеграционный тест находит вопрос по точному редкому слову, который не находит чистый векторный поиск
- [x] 6.3 Реализовать объединение веток через reciprocal rank fusion и нормировку оценки локальных совпадений; проверка — unit-тесты на фиксированных рангах дают ожидаемый порядок и оценку в `[0, 1]`
- [x] 6.4 Реализовать флаг отключения полнотекстовой ветки и деградацию до чистого векторного поиска при ошибке ранжирования; проверка — unit-тесты подтверждают работу обоих режимов и корректную деградацию
- [x] 6.5 Проставить локальным совпадениям `score_kind` и обеспечить раздельный порог по типам оценки; проверка — unit-тест подтверждает, что порог применяется по своему типу оценки

## 7. Внешний источник gotquestions.online

- [x] 7.1 Сохранить в тестовые фикстуры HTML выдачи поиска (непустую, пустую, отвергнутую и с изменённой разметкой) и страницы вопроса внешнего источника; проверка — фикстуры лежат в репозитории и используются контрактными тестами
- [x] 7.2 Реализовать интерфейс `ExternalQuestionSource` и адаптер gotquestions.online: GET-поиск с параметрами `search`, `type`, `page`, `limit` и разбор выдачи; проверка — контрактный тест на непустой фикстуре извлекает список вопросов с текстом, ответом, комментарием, автором и ссылкой `/question/<id>`
- [x] 7.3 Реализовать обязательный настроенный `user_agent`, соблюдение `robots.txt`, ограничение частоты запросов и настраиваемый таймаут; проверка — unit-тесты: `403` на недопустимый `User-Agent` классифицируется как `unavailable`, а интервал между запросами не меньше заданного минимума
- [x] 7.4 Сделать интеграцию включённой по умолчанию (`enabled=true`) и реализовать circuit breaker с выключателем для эксплуатации; проверка — unit-тест: при настройках по умолчанию запрос к внешнему сайту выполняется, после серии ошибок ветка сразу возвращает `unavailable`, а при `enabled=false` запросы не выполняются
- [x] 7.5 Реализовать различение четырёх исходов внешнего поиска — непустая выдача, пустая выдача, отвергнутый запрос и недоступность; проверка — unit-тесты на фикстурах: непустая выдача даёт совпадения со статусом успеха, «Ничего не найдено» даёт `empty`, страница без блока результатов даёт `rejected`, битая разметка и `5xx` дают `unavailable`
- [x] 7.6 Реализовать пагинацию внешней выдачи через `page`/`limit` с ограничением числа запрашиваемых страниц; проверка — unit-тест подтверждает, что выдача нескольких страниц объединяется без дублирования совпадений и не превышает настроенный лимит страниц
- [x] 7.7 Реализовать формирование короткого поискового запроса (не более 50 символов) из длинного описания: извлечение ключевых терминов, при необходимости несколько запросов на одно описание; проверка — unit-тесты: короткое описание уходит без изменений, длинное даёт запрос не длиннее 50 символов с ключевыми терминами, ни один запрос не превышает лимит
- [x] 7.8 Обработать деградацию парсера и дополнить метрики пустых, отвергнутых и сокращённых выдач; проверка — unit-тест на фикстуре с изменённой разметкой не возвращает тихий пустой успешный результат, а соответствующие счётчики инкрементируются
- [x] 7.9 Присвоить внешним совпадениям `score_kind=external_rank` и оценку из позиции в выдаче; проверка — unit-тест подтверждает нормировку оценки по позиции и сохранение ссылки на источник

## 8. Граф поиска на LangGraph

- [x] 8.1 Реализовать узлы `parse_request`, `local_search`, `external_search` как обычные async-функции; проверка — unit-тесты каждого узла изолированно, с фейковыми зависимостями
- [x] 8.2 Собрать граф с параллельным fan-out двух поисковых веток и узел `merge_and_dedupe`; проверка — unit-тест подтверждает, что оба источника вызываются параллельно и совпадения дедуплицируются по каноническому вопросу
- [x] 8.3 Реализовать независимые таймауты, условные рёбра деградации и передачу статуса источника (`ok`, `empty`, `rejected`, `unavailable`) и применённого внешнего запроса в итоговый ответ; проверка — unit-тесты с медленным или недоступным внешним источником возвращают локальные результаты с признаком частичного ответа, а с пустой и отвергнутой выдачей — соответствующие статусы
- [x] 8.4 Реализовать узлы `rerank`, `generate` и `format_response` с опциональной генерацией; проверка — unit-тесты подтверждают вызов генерации только при наличии совпадений и корректный формат ответа
- [x] 8.5 Реализовать дедупликацию с сохранением информации обо всех источниках и лучшей оценки; проверка — unit-тест: вопрос, найденный в двух источниках, возвращается одним совпадением с двумя источниками

## 9. HTTP API

- [x] 9.1 Описать pydantic-схемы запросов и ответов поиска, импорта и ошибок; проверка — unit-тесты валидации отвергают пустое описание, слишком длинное описание и недопустимый порог
- [x] 9.2 Реализовать эндпоинт поиска с вызовом графа; проверка — тест `httpx.ASGITransport` возвращает 200 и структурированный результат с совпадениями и статусами источников
- [x] 9.3 Реализовать эндпоинты запуска импорта и получения состояния операции, включая 404 на неизвестный идентификатор; проверка — тесты подтверждают приём операции, получение статуса и 404
- [x] 9.4 Реализовать `/healthz` и `/readyz` с проверкой базы данных; проверка — тест: при недоступной базе `/readyz` отвечает 503 с указанием зависимости, при доступной — 200
- [x] 9.5 Реализовать единый конверт ошибок с `code`, `message`, `request_id` и middleware генерации `request_id`; проверка — тест на внутреннюю ошибку возвращает 500 в едином формате без деталей реализации
- [x] 9.6 Подключить сбор метрик Prometheus и эндпоинт `/metrics`; проверка — тест подтверждает наличие метрик поиска, индексации и использования GigaChat после выполнения операций

## 10. Веб-интерфейс NiceGUI

- [x] 10.1 Смонтировать NiceGUI в FastAPI-приложение и настроить внутрипроцессный вызов API через `ASGITransport`; проверка — тест подтверждает доступность страницы UI через тестовый клиент приложения
- [x] 10.2 Реализовать форму поиска с валидацией пустого и слишком короткого описания и с параметрами числа результатов и порога; проверка — тест подтверждает, что при пустом описании запрос не отправляется
- [x] 10.3 Реализовать отображение состояний ожидания, выполнения, успеха, частичных результатов и ошибки; проверка — тесты на каждый статус показывают соответствующее состояние и идентификатор запроса при ошибке
- [x] 10.4 Реализовать список совпадений с текстом вопроса, ответом, источником, оценкой и типом оценки, а также ссылкой на внешнюю страницу вопроса; проверка — тест подтверждает наличие всех полей и разделение локальных и внешних результатов
- [x] 10.5 Отображать для внешнего поиска статус источника (`ok`, `empty`, `rejected`, `unavailable`) и фактически использованный короткий запрос; проверка — тесты подтверждают, что пустая выдача, отвергнутый запрос и недоступность выглядят по-разному, а сокращённый запрос показан пользователю
- [x] 10.6 Реализовать вывод сгенерированного ответа отдельно от совпадений со ссылками на использованные вопросы и корректное поведение при отсутствии подтверждающих совпадений или недоступной генерации; проверка — тесты на оба сценария

## 11. Наблюдаемость и эксплуатация

- [x] 11.1 Реализовать метрики операций поиска (счётчик, гистограмма длительности, ошибки, частичные результаты) с метками `source` и `status`; проверка — unit-тест на фиксированных операциях даёт ожидаемые значения
- [x] 11.2 Реализовать метрики индексации и метрику размера очереди невекторизованных вопросов; проверка — unit-тест подтверждает обновление счётчиков и значение очереди
- [x] 11.3 Реализовать структурные логи операций поиска и импорта с `request_id`, статусом, длительностью и источниками; проверка — unit-тест проверяет состав полей записи и отсутствие секретов
- [x] 11.4 Обеспечить сквозной идентификатор для операции импорта, связывающий отчёт, логи и метрики; проверка — unit-тест подтверждает наличие одного и того же идентификатора в отчёте и логах
- [x] 11.5 Описать в README локальный запуск (PostgreSQL с pgvector, миграции, `.env`, запуск сервиса) и конфигурацию, включая предупреждение о внешнем источнике, его включённое по умолчанию состояние, обязательный `user_agent`, лимит запроса в 50 символов, rate limiting и инструкцию по отключению; проверка — по инструкции сервис запускается на локальной базе, `/readyz` отвечает 200, а внешний источник фигурирует в ответе со статусом `ok` или `empty`, не `rejected` и не `unavailable`

## 12. Сквозные проверки

- [x] 12.1 Написать end-to-end интеграционный тест: импорт HTML-фикстур, поиск по описанию, получение совпадений из локальной базы и генерация ответа; проверка — тест проходит на реальной базе с pgvector и фейковым GigaChat
- [x] 12.2 Проверить на сквозном уровне сценарии включённой интеграции: недоступность, пустую выдачу и отвергнутый запрос; проверка — во всех случаях поиск отвечает 200 с локальными совпадениями и различающимся статусом внешнего источника, а соответствующие метрики инкрементируются
- [x] 12.3 Проверить на сквозном уровне длинное описание вопроса (несколько предложений); проверка — внешний источник получает запрос не длиннее 50 символов, возвращает совпадения, а локальный поиск использует полное описание
- [x] 12.4 Проверить сценарий смены embedding-модели: смена размерности в конфигурации и команда переиндексации без повторного разбора HTML; проверка — поиск работает после переиндексации, а размерность в схеме согласована с настройкой
- [x] 12.5 Прогнать полный набор тестов и линтеров; проверка — `uv run pytest -q` и выбранный линтер/форматтер проходят без ошибок
- [x] 12.6 Измерить базовые метрики качества поиска на подготовленном наборе «описание → ожидаемый вопрос»; проверка — отчёт фиксирует recall@k и MRR на наборе и сохраняется как артефакт
