## 1. Настройка адреса тестовой базы

- [x] 1.1 Добавить в `DatabaseSettings` настройку `test_dsn` с вычисляемым значением по умолчанию: рабочая настройка с именем базы, дополненным суффиксом `_test`; проверка — unit-тест подтверждает значение по умолчанию для DSN по умолчанию и для явно заданного DSN с параметрами в строке
- [x] 1.2 Реализовать явное задание тестового DSN через `CHGK_DATABASE__TEST_DSN`; проверка — unit-тест подтверждает, что переменная окружения переопределяет значение по умолчанию
- [x] 1.3 Описать `CHGK_DATABASE__TEST_DSN` в `.env.example` и README рядом с блоком о тестах; проверка — переменная присутствует в обоих файлах

## 2. Подготовка тестовой базы

- [x] 2.1 Реализовать вспомогательный модуль подготовки: разбор DSN на административный и целевой адреса, проверка совпадения целевой базы с рабочей, создание базы при отсутствии; проверка — unit-тесты на разбор адресов и на срабатывание проверки совпадения
- [x] 2.2 Реализовать применение миграций Alembic к тестовой базе с явно переданным DSN; проверка — тест подтверждает, что после подготовки в базе есть таблица `questions` с колонкой `embedding` типа `vector(<настройка>)`
- [x] 2.3 Реализовать пропуск подготовки с понятным сообщением, когда базу нельзя создать или к ней нет доступа; проверка — тест с недоступным административным адресом приводит к пропуску, а не к ошибке

## 3. Перевод тестовых фикстур на тестовый DSN

- [x] 3.1 Перевести фикстуры `engine`, `session_factory` и `session` в `tests/conftest.py` на тестовый DSN с подготовкой схемы один раз за сессию; проверка — полный прогон `uv run pytest -q` проходит, а рабочая база не изменяется
- [x] 3.2 Сохранить поведение пропуска интеграционных тестов при недоступной базе; проверка — тест с недоступным адресом помечает интеграционные тесты пропущенными, а модульные выполняются
- [x] 3.3 Убедиться, что фикстура `session` по-прежнему откатывает транзакцию теста и не оставляет данных; проверка — тест, записавший источник и упавший, не оставляет записей в тестовой базе

## 4. Защита рабочей базы

- [x] 4.1 Довести проверку совпадения адресов до отказа запуска с понятным сообщением; проверка — тест с `test_dsn`, равным рабочему DSN, завершается ошибкой с объяснением
- [x] 4.2 Проверить отсутствие подмены рабочей базой: при недоступной тестовой базе интеграционные тесты пропускаются и ни один запрос не уходит в рабочую; проверка — тест с подменённым недоступным адресом не выполняет интеграционных тестов

## 5. Проверка миграций на одноразовой базе

- [x] 5.1 Перевести проверку миграции под изменённую размерность эмбеддинга на одноразовую базу с уникальным именем и удалением в `finally`; проверка — тест проходит и после себя не оставляет базы
- [x] 5.2 Убедиться, что проверка не затрагивает рабочую и основную тестовую базы; проверка — тест сравнивает состояние обеих баз до и после проверки миграции

## 6. Восстановление данных и сквозная проверка

- [x] 6.1 Пересчитать испорченный вектор в рабочей базе командой `uv run chgk-reindex --force`; проверка — в рабочей базе не осталось вопросов с моделью эмбеддинга, отличной от настроенной
- [x] 6.2 Зафиксировать в README порядок работы с тестовой базой: автоматическое создание, права `CREATEDB`, ручная подготовка и переменная `CHGK_DATABASE__TEST_DSN`; проверка — по инструкции полный прогон тестов проходит на чистой машине
- [x] 6.3 Прогнать полный набор тестов и линтеров; проверка — `uv run pytest -q` и `uv run ruff check src tests` проходят без ошибок, а содержимое рабочей базы не меняется по сравнению с состоянием до прогона
