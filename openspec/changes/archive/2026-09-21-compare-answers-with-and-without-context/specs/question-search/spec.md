## MODIFIED Requirements

### Requirement: Формирование ответа на основе найденных вопросов
Система SHALL по запросу формировать два независимых ответа на естественном языке на исходное описание вопроса, используя chat-модель GigaChat через интеграцию `langchain_gigachat`: один ответ без подгрузки похожих вопросов и один ответ с найденными похожими вопросами как необязательным контекстом. Генерация SHALL выполняться безусловно: система MUST NOT принимать признак, позволяющий отключить формирование ответа, и MUST NOT пропускать оба прогона. Формирование ответа без подгрузки SHALL NOT зависеть от результатов поиска: оно MAY выполняться параллельно с поиском. Система MUST NOT отказываться от генерации из-за отсутствия подтверждающих совпадений: если ни один вопрос не прошёл порог похожести, ответ без подгрузки SHALL быть получен, а второй прогон генерации с пустым контекстом MUST NOT выполняться.

#### Scenario: Успешная генерация ответа
- **WHEN** поиск вернул совпадения
- **THEN** система возвращает два ответа: без подгрузки похожих вопросов и с подгрузкой найденных совпадений, причём второй сопровождается ссылками на использованные совпадения

#### Scenario: Ответ без подгрузки не ждёт поиска
- **WHEN** запрос обрабатывается
- **THEN** ответ без подгрузки формируется независимо от результатов поиска и без ожидания их появления

#### Scenario: Генерация не отключается
- **WHEN** приходит запрос на поиск
- **THEN** система формирует ответ и не имеет признака запроса, настройки или ветки графа, которые пропускали бы генерацию целиком

#### Scenario: Нет подтверждающих совпадений
- **WHEN** совпадения отсутствуют
- **THEN** система возвращает ответ без подгрузки, а ответ с подгрузкой остаётся отсутствующим и не выдаётся за сгенерированный

#### Scenario: Ошибка GigaChat при генерации
- **WHEN** GigaChat недоступен или возвращает ошибку во время генерации с подгрузкой
- **THEN** система возвращает найденные совпадения, ответ без подгрузки, если он получен, и признак недоступности генерации с подгрузкой вместо ошибки всего запроса

#### Scenario: Отказ генерации без подгрузки не отменяет поиск
- **WHEN** GigaChat недоступен или возвращает ошибку во время генерации без подгрузки
- **THEN** система возвращает признак недоступности ответа без подгрузки и продолжает поиск, не подменяя его ответом с подгрузкой

#### Scenario: Запуск генерации без совпадений
- **WHEN** ни один вопрос не прошёл порог похожести
- **THEN** граф поиска вызывает генерацию ответа без подгрузки и не выполняет второй прогон генерации с пустым контекстом
