## 1. Два поля ответа в домене и состоянии

- [x] 1.1 Разделить ответ в итоге поиска `src/chgk_agent/search/models.py` на `answer_without_context` и `answer_with_context`; проверить тестом, что отсутствие прогона (`None`) и неудача прогона (`available=False`) различаются
- [x] 1.2 Обновить `SearchState` в `src/chgk_agent/search/state.py` под два поля ответа и убрать из состояния признак генерации; проверить тестом сериализацию состояния с обоими полями и с отсутствующим вторым ответом
- [x] 1.3 Обновить тесты состояния и моделей под новые поля; проверить, что `uv run pytest -q tests/test_search_state.py` проходит

## 2. Два прогона генерации

- [x] 2.1 Разделить сборку промпта в `src/chgk_agent/search/nodes.py` на промпт без подсказок и промпт с подсказками; проверить тестом, что первый не содержит блока «Раньше ты встречал такие похожие вопросы», а второй содержит
- [x] 2.2 Реализовать узел генерации без подгрузки, который не зависит от совпадений; проверить тестом, что узел возвращает ответ при пустом списке совпадений и при недоступном провайдере не бросает исключение
- [x] 2.3 Перевести существующий узел генерации на промпт с подгрузкой и запись в поле ответа с контекстом, а при пустом списке совпадений — на возврат отсутствия ответа без обращения к модели; проверить тестом, что модель не вызывается без контекста и вызывается с ним
- [x] 2.4 Проверить раздельные ошибки прогонов: при отказе одного прогона второй остаётся доступным, а запрос не помечается частичным; проверить тестом с фейком, падающим на одном из вызовов

## 3. Удаление признака генерации

- [x] 3.1 Убрать `generate_answer` из настроек `src/chgk_agent/config.py`; проверить тестом, что настройка больше не существует и конфигурация собирается без неё
- [x] 3.2 Убрать разбор признака и функцию маршрутизации `needs_generation` из `src/chgk_agent/search/nodes.py`; проверить тестом отсутствие ветки, пропускающей генерацию
- [x] 3.3 Убрать параметр и ключ состояния из метода запуска графа в `src/chgk_agent/search/graph.py`; проверить тестом, что граф запускается без признака генерации
- [x] 3.4 Убрать поле из запроса API и из клиента интерфейса; проверить тестом, что запрос с этим полем отклоняется как содержащий неизвестное поле

## 4. Граф: параллельная ветка

- [x] 4.1 Добавить ветку `parse_request → generate_without_context` в один супершаг с подготовительными нодами; проверить тестом с задержками, что интервалы генерации и подготовки перекрываются
- [x] 4.2 Провести вторую ветку генерации после ранжирования и собрать обе в `format_response` через join; проверить тестом, что `format_response` вызывается ровно один раз при наличии и при отсутствии совпадений
- [x] 4.3 Убрать развилку `has_results`: ранжирование выполняется всегда; проверить тестом, что пустая выдача проходит граф без ошибок и без обращения к модели с пустым контекстом
- [x] 4.4 Собрать оба ответа в `format_response` и учесть их в логах; проверить тестом, что итог поиска содержит оба ответа, а лог остаётся корректным при отсутствии второго прогона
- [x] 4.5 Обновить существующие тесты графа под новые поля ответа и удалённый признак; проверить, что `uv run pytest -q tests/test_search_graph.py` проходит

## 5. Контракт API

- [x] 5.1 Заменить в `src/chgk_agent/api/schemas.py` поле `answer` на `answer_without_context` и `answer_with_context`; проверить тестом схему ответа с обоими полями и с отсутствующим вторым
- [x] 5.2 Обновить сборку ответа из результата поиска и маршрут `POST /search`; проверить тестом, что сервис отвечает 200 и передаёт оба результата генерации раздельно
- [x] 5.3 Проверить частичный отказ генерации на уровне API: успешный прогон и признак недоступности неуспешного приходят в одном ответе; проверить тестом с фейковым провайдером
- [x] 5.4 Обновить тесты API и сквозные тесты под новый контракт; проверить, что `uv run pytest -q tests/test_api.py tests/test_end_to_end.py` проходит

## 6. Веб-интерфейс: два ответа рядом

- [x] 6.1 Перевести модель экрана в `src/chgk_agent/ui/view.py` на два подписанных ответа с текстами подписей «Без базы вопросов» и «С учётом базы вопросов»; проверить тестом построение модели для обоих ответов, для одного и для «Не знаю» на месте каждого
- [x] 6.2 Обновить клиент API в `src/chgk_agent/ui/client.py` под новые поля ответа и убрать передачу признака генерации; проверить тестом, что клиент возвращает оба ответа, а при ошибке сервиса — «Не знаю» на месте обоих
- [x] 6.3 Обновить страницу в `src/chgk_agent/ui/app.py`: два подписанных места для ответов, статус «Думаю...» на время запроса, блокировка формы и очистка прежних ответов; проверить тестом порядок «очистить → статус → ответы» и состав страницы
- [x] 6.4 Проверить, что при отсутствии второго прогона страница не показывает выдуманный ответ, а подписи не путаются; проверить тестом с ответом без совпадений
- [x] 6.5 Обновить тесты интерфейса и сквозной тест страницы; проверить, что `uv run pytest -q tests/test_ui.py` проходит

## 7. Проверка и документация

- [x] 7.1 Проверить на живом сервере на одном описании, что рядом видны два различающихся или совпадающих ответа под разными подписями, а форма разблокируется; зафиксировать наблюдение
- [x] 7.2 Обновить README: пример ответа API с двумя полями и описание страницы с двумя ответами; проверить, что пример соответствует реальному ответу сервиса
- [x] 7.3 Прогнать `uv run pytest -q` и `uv run ruff check src tests` без замечаний
