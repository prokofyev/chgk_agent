## 1. Доступ к информативности терминов

- [x] 1.1 Дать индексу доступ к документной частоте термина и функции информативности (`idf`) по основе; проверить тестом, что редкий термин получает большее значение, чем частый, а термин вне корпуса — максимальное
- [x] 1.2 Вынести построение индекса в общий хелпер и перевести на него локальную ветку и ноду графа; проверить тестом, что обе стороны получают один и тот же индекс из кэша
- [x] 1.3 Сохранить ленивое построение внутри локального поиска как запасной путь для прямых вызовов; проверить тестом, что прямой вызов `LocalSearch` строит индекс сам

## 2. Нода подготовки индекса

- [x] 2.1 Добавить в состояние поиска признак информативности терминов описания и причину её недоступности; проверить тестом сериализацию состояния
- [x] 2.2 Реализовать узел подготовки индекса с собственным таймаутом из настроек: построить индекс при необходимости, посчитать информативность основ описания и записать её в состояние; проверить тестом, что узел заполняет состояние и не обращается к базе при готовом индексе
- [x] 2.3 Обработать недоступность базы в узле: записать причину, продолжить без информативности и не бросать исключение; проверить тестом, что узел возвращает результат при падающей сессии
- [x] 2.4 Строить индекс независимо от настройки отключения лексики; проверить тестом, что при отключённой лексике информативность всё равно посчитана

## 3. Граф

- [x] 3.1 Поставить узел подготовки индекса на критический путь до ветвления: `parse_request → (embed_query | ensure_corpus_index) → (local_search | external_search)`; проверить тестом, что обе ветки стартуют после обеих подготовительных нод
- [x] 3.2 Убедиться, что подготовка индекса выполняется параллельно вычислению эмбеддинга; проверить тестом с задержками, что длительность поиска не включает построение индекса последовательно
- [x] 3.3 Проверить, что отказ подготовки индекса не отменяет внешний поиск и помечает ответ частичным; проверить тестом с падающим индексом
- [x] 3.4 Передавать информативность терминов в ветку внешнего поиска; проверить тестом, что источник получает веса, когда индекс доступен, и `None`, когда нет

## 4. Короткий запрос

- [x] 4.1 Добавить необязательный параметр весов терминов в протокол внешнего источника и в реализацию gotquestions.online; проверить тестом, что вызов без весов работает по-прежнему
- [x] 4.2 Переписать отбор терминов на редкость основы: стоп-слова и типовые формулировки отбрасываются до отбора, порядок при равной редкости — по появлению в тексте; проверить тестом, что редкое короткое слово вытесняет частое длинное
- [x] 4.3 Отправлять несколько запросов, по одному термину в каждом: взять самые информативные термины, уложить каждый в лимит длины и ограничить число запросов настройкой; проверить тестом, что число запросов не превышает настройку, в каждом ровно один термин, а длина не превышает 50 символов
- [x] 4.4 Оставить работу без весов: при недоступной информативности отбор идёт прежним правилом; проверить тестом, что запрос формируется и не пуст
- [x] 4.5 Вынести число коротких запросов и таймаут подготовки индекса в настройки; проверить тесты настроек

## 5. Проверка

- [x] 5.1 Обновить и дополнить тесты построителя коротких запросов под отбор по редкости и один термин на запрос
- [x] 5.2 Прогнать `uv run pytest -q` и `uv run ruff check src tests` без замечаний
- [x] 5.3 Проверить вручную на живом сервере на описании про «мучеников»: в логах несколько запросов по одному термину, в них нет служебных слов, а выдача содержит внешние совпадения
- [x] 5.4 Обновить README, если порядок работы с индексом изменился для эксплуатации
